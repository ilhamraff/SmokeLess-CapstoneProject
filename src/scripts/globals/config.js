/**
 * CONFIG adalah objek yang berisi konfigurasi dasar untuk aplikasi,
 * termasuk BASE_URL untuk mengakses API.
 */
const CONFIG = {
  /**
   * BASE_URL adalah URL dasar untuk mengakses API.
   * @type {string}
   */
  BASE_URL: 'https://smokeless-web-default-rtdb.firebaseio.com/',
};

export default CONFIG;
